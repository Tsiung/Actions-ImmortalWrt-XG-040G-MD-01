name: Dual-Branch-Build

on:
  workflow_dispatch:
    inputs:
      configs:
        description: '选择要编译的配置（逗号分隔，支持：base,openclash,ksmbd,cups,full）'
        required: true
        default: 'base'
        type: string
      debug_mode:
        description: '调试模式（仅编译第一个匹配的配置，用于快速测试）'
        required: false
        default: false
        type: boolean

env:
  REPO_URL: https://github.com/wina8201/immortalwrt.git
  DEVICE_TARGET: airoha
  DEVICE_SUBTARGET: an7581
  TZ: Asia/Shanghai

jobs:
  build:
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        # 固定两个分支
        branch: [master, openwrt-25.12]
        # 静态定义所有可能的配置（不再动态生成 JSON）
        config:
          - { seed: "seed-01.config", suffix: "Base", desc: "基础功能", tag: "base" }
          - { seed: "seed-02.config", suffix: "OpenClash", desc: "基础功能 + OpenClash", tag: "openclash" }
          - { seed: "seed-03.config", suffix: "OpenClash-KSMBD", desc: "基础功能 + OpenClash + KSMBD文件共享", tag: "ksmbd" }
          - { seed: "seed-04.config", suffix: "OpenClash-CUPS", desc: "基础功能 + OpenClash + CUPS打印机共享", tag: "cups" }
          - { seed: "seed-05.config", suffix: "OpenClash-KSMBD-CUPS", desc: "基础功能 + OpenClash + KSMBD文件共享 + CUPS打印机共享", tag: "full" }

    runs-on: ubuntu-latest
    steps:
      # ==================== 条件过滤：只运行用户选择的配置 ====================
      - name: 检查当前配置是否被用户选中
        id: filter
        run: |
          SELECTED="${{ github.event.inputs.configs }}"
          TAG="${{ matrix.config.tag }}"
          DEBUG="${{ github.event.inputs.debug_mode }}"

          # 将逗号分隔的字符串转为空格分隔的数组
          IFS=',' read -ra SELECTED_ARRAY <<< "$SELECTED"
          SELECTED_TAGS="${SELECTED_ARRAY[@]}"

          # 检查当前 tag 是否在选中列表中
          MATCH=0
          for s in "${SELECTED_ARRAY[@]}"; do
            if [ "$s" = "$TAG" ]; then
              MATCH=1
              break
            fi
          done

          if [ $MATCH -eq 0 ]; then
            echo "当前配置 $TAG 未被用户选中，跳过"
            echo "skip=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          # 调试模式：只保留第一个匹配的配置
          if [ "$DEBUG" = "true" ]; then
            # 获取选中列表中的第一个 tag
            FIRST_TAG="${SELECTED_ARRAY[0]}"
            if [ "$TAG" != "$FIRST_TAG" ]; then
              echo "调试模式开启，且当前配置 $TAG 不是第一个匹配的配置，跳过"
              echo "skip=true" >> $GITHUB_OUTPUT
              exit 0
            else
              echo "调试模式：保留第一个匹配的配置 $TAG"
            fi
          fi

          echo "skip=false" >> $GITHUB_OUTPUT

      # 如果 skip=true，则后续所有步骤都不会执行（通过条件判断）
      # ===================================================================

      - name: 初始化编译环境
        if: steps.filter.outputs.skip != 'true'
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo bash -c 'bash <(curl -sL https://build-scripts.immortalwrt.org/init_build_environment.sh)'
          sudo -E apt -yqq install dos2unix libfuse-dev
          sudo timedatectl set-timezone "$TZ"

      - name: 扩展磁盘空间
        if: steps.filter.outputs.skip != 'true'
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024

      - name: 检出当前仓库（存放 seed 文件）
        if: steps.filter.outputs.skip != 'true'
        uses: actions/checkout@v4

      - name: 克隆 ImmortalWrt 源码（指定分支）
        if: steps.filter.outputs.skip != 'true'
        run: |
          git clone --depth 1 "$REPO_URL" -b "${{ matrix.branch }}" openwrt
          echo "OPENWRT_PATH=$PWD/openwrt" >> $GITHUB_ENV

      - name: 获取源码分支唯一标识（commit hash）
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd $OPENWRT_PATH
          COMMIT_HASH=$(git rev-parse --short HEAD)
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV

      - name: 生成构建日期及变量
        if: steps.filter.outputs.skip != 'true'
        run: |
          BUILD_DATE=$(TZ=Asia/Shanghai date +"%Y%m%d")
          BUILD_TIME=$(TZ=Asia/Shanghai date +"%Y-%m-%d %H:%M:%S")
          echo "BUILD_DATE=$BUILD_DATE" >> $GITHUB_ENV
          echo "BUILD_TIME=$BUILD_TIME" >> $GITHUB_ENV
          RELEASE_TAG="XG-040G-MD-${{ matrix.branch }}-${{ matrix.config.suffix }}-${BUILD_DATE}-${{ github.run_number }}"
          echo "RELEASE_TAG=$RELEASE_TAG" >> $GITHUB_ENV

      - name: 加载对应种子配置
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd $OPENWRT_PATH
          cp $GITHUB_WORKSPACE/${{ matrix.config.seed }} .config
          make defconfig

      - name: 恢复 DL 缓存
        if: steps.filter.outputs.skip != 'true'
        uses: actions/cache@v4
        with:
          path: ${{ env.OPENWRT_PATH }}/dl
          key: dl-${{ matrix.branch }}-${{ hashFiles('openwrt/feeds.conf*', 'openwrt/.config') }}
          restore-keys: |
            dl-${{ matrix.branch }}-

      - name: 更新 feeds 并安装软件包（基础）
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd $OPENWRT_PATH
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 添加自定义打印软件包源（仅 CUPS 配置）
        if: steps.filter.outputs.skip != 'true' && contains(matrix.config.desc, 'CUPS')
        run: |
          cd $OPENWRT_PATH
          git clone --depth=1 https://github.com/master-0123/openwrt-printing-packages package/printing-packages
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 下载软件包
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd $OPENWRT_PATH
          make download -j8
          find dl -size -1024c -delete

      - name: 编译固件
        id: compile
        if: steps.filter.outputs.skip != 'true'
        run: |
          cd $OPENWRT_PATH
          make -j$(nproc) || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT

      - name: 整理产物
        if: steps.filter.outputs.skip != 'true' && steps.compile.outputs.status == 'success'
        run: |
          cd $OPENWRT_PATH/bin/targets/*/*
          VERSION_KERNEL=$(find -type f -name "*.manifest" -exec grep -oP '^kernel - \K.*?(?=~|-\d+-)' {} \; | head -1)
          echo "VERSION_KERNEL=$VERSION_KERNEL" >> $GITHUB_ENV
          cp $OPENWRT_PATH/.config build.config
          echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV

      - name: 发布固件到独立 Release
        if: steps.filter.outputs.skip != 'true' && steps.compile.outputs.status == 'success'
        uses: ncipollo/release-action@v1
        with:
          name: XG-040G-MD-${{ matrix.branch }}-${{ matrix.config.suffix }}-${{ env.BUILD_DATE }}
          tag: ${{ env.RELEASE_TAG }}
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: ${{ env.FIRMWARE_PATH }}/*.bin,${{ env.FIRMWARE_PATH }}/*.manifest,${{ env.FIRMWARE_PATH }}/build.config
          body: |
            ## XG-040G-MD 固件 (${{ matrix.branch }} / ${{ matrix.config.suffix }})

            ### 📦 构建信息
            | 项目 | 内容 |
            |------|------|
            | **设备型号** | Bell XG-040G-MD |
            | **目标平台** | airoha/an7581 |
            | **源码分支** | ${{ matrix.branch }} (commit ${{ env.COMMIT_HASH }}) |
            | **配置类型** | ${{ matrix.config.desc }} |
            | **源码来源** | [wina8201/immortalwrt](https://github.com/wina8201/immortalwrt) (forked from fzs209) |
            | **编译时间** | ${{ env.BUILD_TIME }} |
            | **内核版本** | ${{ env.VERSION_KERNEL }} |

            ### 🗂️ 文件说明
            - `*-squashfs-sysupgrade.bin` → 在已有 OpenWrt/ImmortalWrt 中升级使用
            - `*-squashfs-factory.bin` → 从 uboot 首次刷机使用
            - `build.config` → 本次编译使用的完整配置文件

            ### 🔐 默认信息
            | 项目 | 默认值 |
            |------|--------|
            | **管理地址** | 192.168.1.1 |
            | **用户名** | root |
            | **密码** | *无（首次登录后请务必设置）* |

            ### ⚠️ 注意事项
            - 刷机前请务必核对设备型号，错误固件可能导致变砖。
            - 建议在刷机前备份原厂固件和重要数据。
            - 若固件内集成第三方插件，其安全性请自行评估。
            - 本固件为自动编译生成，请仔细核对文件完整性后再刷写。
            ${{ contains(matrix.config.desc, 'CUPS') && '\n- **打印机驱动说明**：本固件集成了 CUPS 打印服务，默认包含 Epson 打印机驱动 (epson-inkjet-printer-escpr)，如需使用其他品牌打印机，请自行安装对应驱动。' || '' }}

            ---
            *由 GitHub Actions 自动构建 | Release ${{ env.RELEASE_TAG }}*
