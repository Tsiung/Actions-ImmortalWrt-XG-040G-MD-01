name: Dual-Branch Build

on:
  workflow_dispatch:
    inputs:
      configs:
        description: '选择要编译的配置（用逗号分隔，可多选）'
        required: true
        default: 'base'
        type: string

env:
  REPO_URL: https://github.com/wina8201/immortalwrt.git
  DEVICE_TARGET: airoha
  DEVICE_SUBTARGET: an7581
  TZ: Asia/Shanghai

jobs:
  build:
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        branch: [master, openwrt-25.12]
        config:
          - {key: base,     seed: seed-01.config, suffix: Base,                  desc: "基础功能",                                     need_cups: false}
          - {key: openclash, seed: seed-02.config, suffix: OpenClash,            desc: "基础功能 + OpenClash",                         need_cups: false}
          - {key: ksmbd,    seed: seed-03.config, suffix: OpenClash-KSMBD,       desc: "基础功能 + OpenClash + KSMBD文件共享",         need_cups: false}
          - {key: cups,     seed: seed-04.config, suffix: OpenClash-CUPS,        desc: "基础功能 + OpenClash + CUPS打印机共享",        need_cups: true}
          - {key: full,     seed: seed-05.config, suffix: OpenClash-KSMBD-CUPS,  desc: "基础功能 + OpenClash + KSMBD文件共享 + CUPS打印机共享", need_cups: true}

    runs-on: ubuntu-latest
    steps:
      - name: 检查是否选中该配置
        if: |
          !contains(github.event.inputs.configs, matrix.config.key)
        run: |
          echo "未选中配置 ${{ matrix.config.key }}，跳过"
          exit 0

      - name: 初始化编译环境
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo bash -c 'bash <(curl -sL https://build-scripts.immortalwrt.org/init_build_environment.sh)'
          sudo -E apt -yqq install dos2unix libfuse-dev
          sudo timedatectl set-timezone "$TZ"

      - name: 扩展磁盘空间
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024

      - name: 检出当前仓库（存放 seed 文件）
        uses: actions/checkout@v4

      - name: 克隆 ImmortalWrt 源码（指定分支）
        run: |
          git clone --depth 1 "$REPO_URL" -b "${{ matrix.branch }}" openwrt
          echo "OPENWRT_PATH=$PWD/openwrt" >> $GITHUB_ENV

      - name: 获取源码分支长标识（commit hash）
        run: |
          cd $OPENWRT_PATH
          COMMIT_HASH=$(git rev-parse --short HEAD)
          echo "COMMIT_HASH=$COMMIT_HASH" >> $GITHUB_ENV

      - name: 生成构建日期及变量
        run: |
          BUILD_DATE=$(TZ=Asia/Shanghai date +"%Y%m%d")
          BUILD_TIME=$(TZ=Asia/Shanghai date +"%Y-%m-%d %H:%M:%S")
          echo "BUILD_DATE=$BUILD_DATE" >> $GITHUB_ENV
          echo "BUILD_TIME=$BUILD_TIME" >> $GITHUB_ENV
          RELEASE_TAG="XG-040G-MD-${{ matrix.branch }}-${{ matrix.config.suffix }}-${BUILD_DATE}-${{ github.run_number }}"
          echo "RELEASE_TAG=$RELEASE_TAG" >> $GITHUB_ENV

      - name: 加载对应种子配置
        run: |
          cd $OPENWRT_PATH
          cp $GITHUB_WORKSPACE/${{ matrix.config.seed }} .config
          make defconfig

      - name: 恢复 DL 缓存
        uses: actions/cache@v4
        with:
          path: ${{ env.OPENWRT_PATH }}/dl
          key: dl-${{ matrix.branch }}-${{ hashFiles('openwrt/feeds.conf*', 'openwrt/.config') }}
          restore-keys: |
            dl-${{ matrix.branch }}-

      - name: 添加打印机驱动包（如果需要 CUPS）
        if: matrix.config.need_cups == true
        run: |
          cd $OPENWRT_PATH
          git clone --depth=1 https://github.com/master-0123/openwrt-printing-packages.git package/printing-packages
          echo "已将打印机驱动包克隆到 package/printing-packages"

      - name: 更新 feeds 并安装软件包
        run: |
          cd $OPENWRT_PATH
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 下载软件包
        run: |
          cd $OPENWRT_PATH
          make download -j8
          find dl -size -1024c -delete

      - name: 编译固件
        id: compile
        run: |
          cd $OPENWRT_PATH
          make -j$(nproc) || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT

      - name: 整理产物
        if: steps.compile.outputs.status == 'success'
        run: |
          cd $OPENWRT_PATH/bin/targets/*/*
          VERSION_KERNEL=$(find -type f -name "*.manifest" -exec grep -oP '^kernel - \K.*?(?=~|-\d+-)' {} \; | head -1)
          echo "VERSION_KERNEL=$VERSION_KERNEL" >> $GITHUB_ENV
          cp $OPENWRT_PATH/.config build.config
          echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV

      - name: 发布固件到独立 Release
        if: steps.compile.outputs.status == 'success'
        uses: ncipollo/release-action@v1
        with:
          name: XG-040G-MD-${{ matrix.branch }}-${{ matrix.config.suffix }}-${{ env.BUILD_DATE }}
          tag: ${{ env.RELEASE_TAG }}
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: ${{ env.FIRMWARE_PATH }}/*.bin,${{ env.FIRMWARE_PATH }}/*.manifest,${{ env.FIRMWARE_PATH }}/build.config
          body: |
            ## XG-040G-MD 固件 (${{ matrix.branch }} / ${{ matrix.config.suffix }})

            ### 📦 构建信息
            | 项目 | 内容 |
            |------|------|
            | **设备型号** | Bell XG-040G-MD |
            | **目标平台** | airoha/an7581 |
            | **源码分支** | ${{ matrix.branch }} (commit ${{ env.COMMIT_HASH }}) |
            | **配置类型** | ${{ matrix.config.desc }} |
            | **源码来源** | [wina8201/immortalwrt](https://github.com/wina8201/immortalwrt) (forked from fzs209) |
            | **编译时间** | ${{ env.BUILD_TIME }} |
            | **内核版本** | ${{ env.VERSION_KERNEL }} |

            ### 🗂️ 文件说明
            - `*-squashfs-sysupgrade.bin` → 在已有 OpenWrt/ImmortalWrt 中升级使用
            - `*-squashfs-factory.bin` → 从 uboot 首次刷机使用
            - `build.config` → 本次编译使用的完整配置文件

            ### 🔐 默认信息
            | 项目 | 默认值 |
            |------|--------|
            | **管理地址** | 192.168.1.1 |
            | **用户名** | root |
            | **密码** | *无（首次登录后请务必设置）* |

            ### ⚠️ 注意事项
            - 刷机前请务必核对设备型号，错误固件可能导致变砖。
            - 建议在刷机前备份原厂固件和重要数据。
            - 若固件内集成第三方插件，其安全性请自行评估。
            - 本固件为自动编译生成，请仔细核对文件完整性后再刷写。
            ${{ contains(matrix.config.desc, 'CUPS') && '\n- **打印机驱动说明**：本固件集成了 CUPS 打印服务，默认包含 Epson 打印机驱动 (epson-inkjet-printer-escpr)，如需使用其他品牌打印机，请自行安装对应驱动。' || '' }}

            ---
            *由 GitHub Actions 自动构建 | Release ${{ env.RELEASE_TAG }}*
